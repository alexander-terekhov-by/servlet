package test.pack.weather.core.entity;

/**
 * @author Alexander Terekhov
 */
public class City {
    public City(String code) {
        // or use some RequestParser if logic is complicated
    }

    //city info as not just plain string
}
